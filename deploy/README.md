# Deploy

Infrastructure and deployment configuration for on-premise execution.

- `Dockerfile.backend` — backend container, optimized for AMD ROCm.
- `Dockerfile.frontend` — frontend web interface container.
- `docker-compose.yml` — local system orchestrator (on-premise).
